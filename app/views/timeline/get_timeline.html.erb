<h1> Hey this is your timeline </h1>
<%= constant_string %>
<br>
<%= form_for @post, url: create_post_remote_path, remote: true do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="">
    <%= f.submit %>
  </div>
   <span id="letters">0</span>
<% end %>
<br>

<% form_tag '/', method: :get do %>

    <%=   autocomplete_field_tag 'email', '', autocomplete_user_email_path, :size => 20 %>
    <br>
    <%= submit_tag 'search' %>
<% end %>


<% if @posts!=nil%>
  <div class="posts-parent-container">
    <% @posts.each do |post| %>
      <div class="post-container">
        <%= render 'post' , post: post %>
      </div>  
    <% end %>
  </div>  
<% end %>
